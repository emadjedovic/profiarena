<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Interview Scheduled</title>
</head>
<body>
  <h1>Hello <%= name %>,</h1>
  <p>Your interview is scheduled on <strong><%= proposedTime %></strong>.</p>
  <% if (isOnline) { %>
    <p>This will be an online interview.</p>
  <% } else { %>
    <p>It will take place at <%= city %>, <%= streetAddress %>.</p>
  <% } %>
  <p>Good luck!</p>
</body>
</html>

<!--
AFTER INTERVIEW CREATION SEND:

sendEmail(
  'talent@example.com',
  'Interview Scheduled',
  'interview-scheduled',
  {
    name: 'John Doe',
    proposedTime: '2024-12-01 10:00 AM',
    isOnline: false,
    city: 'Sarajevo',
    streetAddress: '123 Example Street'
  }
);

-->